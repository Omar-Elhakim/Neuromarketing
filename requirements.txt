# Preprocessing + EDA only (src/, preprocessing/).
# The deep-learning experiments need older numpy/scipy -- see
# requirements-experiments.txt, and install it in a separate environment.
numpy==2.3.4
pandas==2.3.3
plotly==6.3.1
scipy==1.16.2
openpyxl  # pandas.read_excel backend for SEED-IV/Channel Order.xlsx
